service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if resource.data.show != false;
      allow write: if request.auth != null;
    }
  }
}